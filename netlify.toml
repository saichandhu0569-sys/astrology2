[build]
  command = "npm run build"
  publish = "dist"

# ✅ Redirect all routes to index.html (fixes blank page issue in React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
